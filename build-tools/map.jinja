{% set build_tools = salt['grains.filter_by']({
    'Debian': {
        'package': 'build-essential',
        'action': 'installed'
    },
    'RedHat': {
        'package': 'Development Tools',
        'action': 'group_install'
    },
}, merge=salt['pillar.get']('build_tools:lookup')) %}