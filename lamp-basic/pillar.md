# Pillar options:

```yaml
lamp-basic:
  host: <optional> # defaults to *
  port: <optional> # defaults to 80
  document_root: <optional> # defaults to /var/www
  server_name: cloudseed.com
  server_alias: www.cloudseed.com
  server_admin: aventurella@gmail.com
  allow_override: All
  options:
    - FollowSymLinks
    - Indexes
```
