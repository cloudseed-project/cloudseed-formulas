# Pillar options:

```yaml
rabbitmq:
  env: <optional> # Path to rabbitmq-env.conf Defaults to salt://rabbitmq/files/rabbitmq-env.conf
  conf: <optional> # Path to rabbitmq.config Defaults to salt://rabbitmq/files/rabbitmq.config
```
